{
	"name": "thai-wine-value-finder",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build:static": "python3 build_static.py",
		"deploy": "npm run build:static && wrangler deploy",
		"dev": "npm run build:static && wrangler dev",
		"docker:build": "docker build --platform linux/amd64 -t thai-wine-value-finder ."
	},
	"devDependencies": {
		"wrangler": "^4.113.0"
	}
}
